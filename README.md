# Gemini YOLO Mode

🏎️ **Auto-accept terminal commands in Gemini**

## What it does
- Automatically accepts terminal/console commands every 2 seconds.
- **Does NOT** auto-accept implementation plans (safety feature).
- **Enabled by default.** Auto-starts automatically.
- To pause/disable: Run `YOLO: Toggle Auto-Accept`.
